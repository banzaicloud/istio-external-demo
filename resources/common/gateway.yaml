apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: egress-external
  namespace: external
spec:
  selector:
    gateway-name: egress-external
    gateway-type: egress
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
