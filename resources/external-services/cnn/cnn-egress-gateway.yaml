apiVersion: istio.banzaicloud.io/v1beta1
kind: MeshGateway
metadata:
  name: egress-cnn
  namespace: external
spec:
  maxReplicas: 1
  minReplicas: 1
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  serviceType: ClusterIP
  type: egress
  labels:
    app: egress-cnn
---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: egress-cnn
  namespace: external
spec:
  selector:
    gateway-name: egress-cnn
    gateway-type: egress
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
