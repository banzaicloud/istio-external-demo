apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: echo-b-google
  namespace: default
spec:
  hosts:
    - google.com
  http:
    - match:
        - port: 80
          sourceLabels:
            k8s-app: echo-b
      route:
        - destination:
            host: egress-external.external.svc.cluster.local
            port:
              number: 80
